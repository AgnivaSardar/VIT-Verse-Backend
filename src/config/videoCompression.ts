// Video compression config for uploads
export const videoCompressionConfig = {
  maxWidth: 1280, // Default max width
  maxHeight: 720, // Default max height
  videoBitrate: '1500k', // Default video bitrate
  crf: 23, // Default CRF (quality)
  preset: 'medium', // Default ffmpeg preset
};
